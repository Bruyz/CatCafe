import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    topo: {
        padding: 20,
        color: 'white',
        fontSize: 40
    },
    semitopo: {
        color: 'white',
        fontSize: 45
    }
})